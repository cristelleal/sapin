server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nowelle_dev
    username: ${dbName}
    password: ${dbMdp}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect